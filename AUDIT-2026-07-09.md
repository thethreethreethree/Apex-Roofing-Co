# Ground-up audit — 2026-07-09

Performed per **§1.7** (ground-up auditing) in the outside-view stance, foundation → top.
Findings are on the record with severity and resolution. "Flags, not blockers."

## Layers walked

| Layer | State | Notes |
|---|---|---|
| Toolchain / deps | ✅ solid | Cloud deps (`db-postgres`, `storage-vercel-blob`, `email-resend`, `@vercel/blob`) removed; `Dockerfile`/`docker-compose.yml`/`vercel.json`/`.dockerignore` gone. Verified. |
| Config (`payload.config.ts`) | ⚠️ → fixed | SQLite-only, no cloud adapters. **Flag H1** (secret) fixed below. |
| Data / media | ✅ verified | `Media.staticDir` → `<project>/media`; `/api/media/file/<f>` returns `200 image/png`; 25 files on disk. |
| Schema / access control | ✅ solid | `leads`/`bookings` read = admin-only, public create disabled (server-action only); `bookings` capacity guard hook (double-book defense). |
| Server actions | ⚠️ → fixed | Honeypot (prior commit) + **Flag M1** (rate limit) fixed below. |
| Presentation / SEO | ⚠️ low | **Flag L1** below. Home/packages/services/book render 200; no roofing strings. |
| HTTP hardening | ⚠️ → fixed | **Flag M2** (security headers) fixed below. |

## Findings & resolutions

- **H1 (HIGH) — `PAYLOAD_SECRET` defaulted to `''`.** An empty secret signs forgeable
  admin sessions. **Fixed:** fail-fast `throw` when `NODE_ENV=production` and the secret
  is unset; dev keeps a fixed fallback. *Verified:* prod+no-secret throws the exact
  message; prod+secret loads; dev+no-secret loads.
- **M1 (MED) — no rate limit on public forms.** Honeypot stops dumb bots only. **Fixed:**
  per-IP fixed-window limiter (`src/lib/ratelimit.ts`), 5/min on `submitLead` and
  `createBooking`, keyed off `x-forwarded-for`. *Verified:* unit test (5 pass, 6th blocked,
  separate key independent). *Untested:* real multi-IP HTTP behavior behind Caddy.
- **M2 (MED) — no security headers.** **Fixed:** `X-Content-Type-Options: nosniff`,
  `X-Frame-Options: SAMEORIGIN`, `Referrer-Policy`, `X-DNS-Prefetch-Control` for all routes
  (no strict CSP — the Payload admin needs inline scripts). *Verified:* headers served on `/`.
- **L1 (LOW) — `robots`/`sitemap` fall back to `http://localhost:3000`** when
  `NEXT_PUBLIC_SITE_URL` is unset. **Not changed:** `DEPLOY.md` sets the env var; behavior is
  correct once set. Left as a documented flag rather than adding indirection.

## Verification summary (this audit)
`tsc --noEmit` clean · `next build` exit 0 (15 routes) · rate-limit unit test pass ·
secret guard 3-case test pass · security headers served · media serves `200 image/png`.
Not run this pass: the Playwright e2e / vitest int suites (see follow-up).
